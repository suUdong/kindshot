version: "3.8"

services:
  kindshot:
    build: .
    container_name: kindshot
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["python", "-m", "kindshot", "--paper"]
