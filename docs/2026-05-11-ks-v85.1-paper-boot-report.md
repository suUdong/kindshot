# KS v85.1 Local Paper Daemon Boot Report (2026-05-11 08:30 KST)

## Daemon
- PID: 324287 (`/home/wdsr88/workspace/kindshot/.venv/bin/python -m kindshot --paper`)
- Mode: paper (KIS_IS_PAPER=true), VTS price snapshots (no real-time)
- Boot time: 2026-05-11 08:27:14 KST
- Uptime at report: ~3 min
- Log file: `logs/v85.1-paper-20260511.log`

## Strategy Registry (boot log)

```
Strategy registered: news               (source=NEWS,      enabled=True)
Strategy registered: rebalance          (source=COMPOSITE, enabled=True)
Strategy registered: alpha_feed         (source=ALPHA,     enabled=True)
inactive_but_requested = ['dart_buyback', 'dart_earnings']   (DART_API_KEY missing)
disabled_by_config     = ['short_overheating', 'technical']
```

3 active strategies on consumer side. v85.1 stagnation_exit defaults active.

## Feed Status Table

| Feed | Enabled | Boot log present | First signal | Reason if silent |
| --- | --- | --- | --- | --- |
| News (KIS poll) | ✅ | ✅ Feed source: KIS,AnalystFeed,Y2I | ✅ 6 poll batches in 3 min | active |
| AnalystFeed | ✅ default | ✅ AnalystFeed enabled (interval=30s) | ✅ 5 normalized headlines | active |
| Y2iFeed | ✅ via env | ✅ Y2iFeed enabled (path=…, min_score=55, interval=60s) | ❌ 0 signals | y2i kindshot_feed.json `generated_at=2026-04-24T13:20Z` (17일 stale) — y2i_lookback_days=3 밖. Producer 측 갱신 필요. |
| RebalanceFeed | ✅ default | ✅ RebalanceFeed registered + starting | ❌ 0 signals | 윈도우 6월/12월 한정. 현재 5월 → 코드상 의도된 silence. |
| AlphaFeed | ✅ via env | ✅ AlphaFeed registered (base_url=http://127.0.0.1:8765, min_confidence=78) | ❌ 0 signals (1 fetch error) | `alpha-scanner serve-kindshot-api` 미가동. Connection refused. |
| DART (buyback) | ❌ inactive | "missing: DART_API_KEY, buyback_queue" | n/a | DART_API_KEY 미설정 |
| DART (earnings, PEAD Phase 1) | ❌ inactive | "missing: DART_API_KEY, earnings_queue" | n/a | DART_API_KEY 미설정 |
| MacroFilter | ✅ default | (lazy: only on BUY decisions) | ❌ 0 evaluations | BUY 결정이 아직 없어 pipeline.py:1053 분기에 도달 못함. Producer endpoint 127.0.0.1:8000은 ALIVE (regime=neutral, conf=0.06). |

## Decisioning

- `events_seen=9` at 08:29:42 heartbeat (8 KIS news + 1 analyst)
- `PAPER BUY/SKIP` 카운트 = 1 (SKIP rule_fallback: conf=55, headline 패턴 매치)
- 새 paper trade insert 0건 → `data/trade_history.db` 행 수 14 변동 없음 (모두 2026-03-27 이전)

## Blockers (외부)

1. **Anthropic credit balance too low** — `LLM circuit breaker OPEN for 3600s`. 거의 모든 분류가 `rule_fallback`로 폴백됨. 정상 sim→real 검증을 위해선 credit 충전 또는 NVIDIA fallback (`NVIDIA_API_KEY`) 활성화 필요.
2. **alpha-scanner serve-kindshot-api 미가동** — 포트 8765 Connection refused. AlphaFeed 등록은 되었으나 데이터 0건.
3. **y2i kindshot_feed.json stale** — 마지막 생성 2026-04-24. 17일 정체. y2i 스케줄러 PID 141851는 살아있지만 KS용 export 갱신 안 됨.
4. **DART_API_KEY 미설정** — DartFeed/DartEarningsStrategy 전부 비활성.
5. **VTS price 모드** — `KIS_REAL_APP_KEY/SECRET` 미설정으로 가격이 stale. t5m_loss_exit 자동 비활성. paper 시뮬 정밀도 ↓.

## Baseline (sim → real comparison floor)

- 기존 trade_history.db: 14건, 2026-03-19 ~ 03-27 구간
- v85.1 deploy 이후 시점부터 increment 측정 가능 (현재까지 +0)
- 60-min checkpoint (~09:30 KST, 장 시작 후 30분): row 증가 확인 예정
- Anthropic credit 미해결 시 BUY 발생률 극저 — credit 보충 후 재측정 권장

## Pytest
- `1341 passed, 1 skipped` in 29.44s on main @ e41f388

## 권장 후속

1. Anthropic 크레딧 충전 또는 `NVIDIA_API_KEY` 추가
2. alpha-scanner 측에 `alpha-scanner serve-kindshot-api --port 8765 &` 실행 요청
3. y2i 스케줄러 측에 kindshot_feed.json 재생성 트리거 (또는 `Y2I_LOOKBACK_DAYS=30`으로 임시 확장)
4. `.env`에 `DART_API_KEY` 추가 (시크릿이므로 commit 금지)
5. `KIS_REAL_APP_KEY/SECRET` 추가하여 VTS 모드 해제
