# v78 Guardrail Profitability Validation

생성일: 2026-03-29 05:28

## 1. Validation Scope

- throughput source: `reports/guardrail_sim.json` (229 guardrail-eligible events, v77 vs v78 비교)
- profitability source: `reports/signal-backtest-result.md` 상세 테이블 42행
- price verification source: `pykrx` 종가 재조회

## 2. 910a331 산출물 점검

- `910a331` 리포트는 `total BUY 87`, `deduped pass 42`, `raw blocked 32`를 함께 표기한다.
- 위 숫자를 같은 분모로 보면 안 된다. raw pass는 `55`건으로 추정되며, 이 중 `13`건이 same-day same-ticker dedupe로 제거된 것이다.
- 따라서 `42 + 32 != 87`은 데이터 오류라기보다 summary framing 문제다. throughput 판단에는 raw event 기반 `guardrail_sim.json` 을, profitability 판단에는 deduped 42행을 사용해야 한다.

## 3. Return Reverification

- `pykrx`로 재조회한 종가 기준 검증 대상: 42행 / price series 39개 / trading days 8일
- entry/T+1/T+5 mismatch: 0건

## 4. Block Rate Comparison

| Metric | v77 | v78 | Delta |
|--------|-----|-----|-------|
| Pass count | 132 | 136 | +4 |
| Pass rate | 57.6% | 59.4% | +1.7%p |
| Block count | 97 | 93 | -4 |
| Block rate | 42.4% | 40.6% | -1.7%p |

주요 관찰:
- throughput 기준 완화 효과는 `+4` pass, `+1.7%p` pass rate, `-4` block 이다.
- raw event 기준 차단률은 `42.4% -> 40.6%` 로 낮아졌지만, 목표였던 `40~60%` 밴드 안에서의 변화 폭은 작다.

## 5. Profitability Decomposition

| Cohort | Matured trades | Win rate | Avg ret | Total ret | Median |
|--------|----------------|----------|---------|-----------|--------|
| All T+1 | 24 | 12.5% | -4.25% | -101.95% | -5.34% |
| All T+5 | 23 | 47.8% | 2.39% | 54.95% | -2.16% |
| Baseline PASSED T+1 | 7 | 0.0% | -4.28% | -29.96% | -5.19% |
| Baseline PASSED T+5 | 7 | 42.9% | -0.81% | -5.67% | -2.16% |
| Newly admitted T+1 | 17 | 17.6% | -4.23% | -71.99% | -5.50% |
| Newly admitted T+5 | 16 | 50.0% | 3.79% | 60.62% | 0.75% |

새로 편입된 시그널 원래 차단 사유:

| Original guardrail | Count |
|--------------------|-------|
| INTRADAY_VALUE_TOO_THIN | 13 |
| LOW_CONFIDENCE | 6 |
| ORDERBOOK_TOP_LEVEL_LIQUIDITY | 4 |
| CHASE_BUY_BLOCKED | 2 |
| OPENING_LOW_CONFIDENCE | 2 |
| FAST_PROFILE_LATE_ENTRY | 2 |
| MARKET_CLOSE_CUTOFF | 1 |

핵심 관찰:
- T+1은 baseline(-4.28%)과 newly admitted(-4.23%) 모두 부진하다.
- T+5는 baseline cohort가 `-0.81%`, newly admitted cohort가 `+3.79%` 로 갈린다.
- `910a331` 표본에서는 완화로 추가된 시그널이 T+5 총합 `60.62%` 를 만들어 전체 평균을 `-0.81% -> +2.39%` 로 바꿨다. 다만 이는 관측 표본 설명이지, 미래 기대수익 일반화는 아니다.

## 6. Expected Return Simulation

가정: `guardrail_sim.json` 의 `+4` extra pass가 `signal-backtest-result.md` 의 newly admitted cohort와 비슷한 수익률 분포를 가진다고 본다.

- extra pass count: 4 / eligible events: 229
- proxy newly admitted T+1 avg: -4.23%
- proxy newly admitted T+5 avg: 3.79%
- implied extra gross T+1 return sum: -16.92%
- implied extra gross T+5 return sum: 15.16%
- implied per-eligible-event uplift at T+1: -0.07%
- implied per-eligible-event uplift at T+5: 0.07%

bootstrap sanity check:
- All T+1 mean: mean -4.25%, 90% bootstrap interval [-5.57%, -2.86%]
- All T+5 mean: mean 2.39%, 90% bootstrap interval [-1.53%, 6.81%]
- Newly admitted T+1 mean: mean -4.23%, 90% bootstrap interval [-5.97%, -2.39%]
- Newly admitted T+5 mean: mean 3.79%, 90% bootstrap interval [-1.55%, 9.80%]

## 7. Verdict

- `910a331` 의 상세 테이블은 현재 시점 `pykrx` 종가로 재검산했을 때 entry/T+1/T+5 mismatch가 없었다.
- 다만 commit 본문/기존 리포트는 raw throughput 수치와 deduped profitability 수치를 같은 summary에 섞어 써서 해석 혼선이 있다.
- v78 완화는 throughput 측면에서는 `+1.7%p` 개선에 그쳤다.
- profitability 측면에서는 표본 내 newly admitted cohort가 T+5에서 유의미한 개선 신호를 보였지만, bootstrap 90% 구간이 `[-1.55%, 9.80%]` 로 넓어 과신하면 안 된다.
- 결론적으로 `v78 완화가 수익성 개선 가능성을 열었다` 까지는 지지되지만, `안정적으로 기대수익이 양수로 전환됐다` 고 단정할 근거는 아직 부족하다.

## 8. Risks

- `trade_history.db` 부재로 `scripts/backtest_signals.py` 를 현 워크트리에서 재실행하지 못했다.
- throughput 비교(`guardrail_sim.json`)와 profitability 비교(`signal-backtest-result.md`)는 서로 다른 표본면을 사용한다.
- newly admitted cohort의 T+5 개선은 일부 큰 winner(`ORDERBOOK_TOP_LEVEL_LIQUIDITY`, `MARKET_CLOSE_CUTOFF`)에 민감하다.

*Generated by `v78_guardrail_profitability_validation.py` → `reports/v78-guardrail-profitability-validation.md`*
